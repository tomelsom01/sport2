module.exports = {
  content: ["./resources/views/*.html", "./resources/js/app.js"],
  theme: {
    extend: {},
  },
  plugins: [],
}
// This is a Tailwind CSS configuration file. It specifies the content files to scan for class names, extends the default theme, and includes any necessary plugins.
// The `content` array includes paths to HTML, JavaScript, and CSS files in the `resources` directory.
// The `theme` object allows you to customize the default Tailwind CSS theme.
// The `plugins` array can be used to include additional Tailwind CSS plugins.
// The configuration is exported as a module, making it available for use in the Tailwind CSS build process.
